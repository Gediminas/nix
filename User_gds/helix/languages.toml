# language-id: https://microsoft.github.io/language-server-protocol/specifications/lsp/3.17/specification/#textDocumentItem

[[language]]
name = "c"
indent = { tab-width = 4, unit = "...." }
auto-format = false
# diagnostic-severity = "Error"
# language-servers = [ { name = "clangd", except-features = [ "format", "diagnostics" ] }, "" ]
#language-server.command = ""

[[language]]
name = "cpp"
language-id = "cpp"
indent = { tab-width = 4, unit = "...." }
auto-format = false
#language-server.command = ""

[[language]]
name = "markdown"
indent = { tab-width = 4, unit = "\t" }
# soft-wrap.enable = true

# [[language]]
# name = "rust"
# # language-id = ""
# diagnostic-severity = "Hint"

# # disable LSP - https://github.com/helix-editor/helix/issues/6686
# [[language]]
# name = "bash"
# # language-id = ""
# indent = { tab-width = 4, unit = "    " }
# formatter = { command = 'shfmt', args = ["-i", "4"] }

# # # expected one of `name`, `scope`, `file-types`, `shebangs`, `roots`, `comment-token`, `text-width`, `soft-wrap`, `config`, `auto-format`, `formatter`, `diagnostic-severity`, `grammar`, `injection-regex`, `language-server`, `indent`, `debugger`, `auto-pairs`, `rulers`, `workspace-lsp-roots`
# # #`in `language`
